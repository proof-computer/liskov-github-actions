{
  "name": "liskov-github-actions",
  "version": "1.3.2",
  "private": true,
  "type": "module",
  "description": "Reusable GitHub Actions + workflows for Liskov CI: manifest-bound artifact and runtime-image publication plus marketplace OIDC ingest.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'actions/**/*.test.ts' && python3 -m unittest discover -s actions/cargo-runtime-image-build/tests -v && python3 -m unittest discover -s actions/source-assurance/tests -v",
    "build": "tsx scripts/build-actions.ts",
    "check:dist": "tsx scripts/build-actions.ts && git diff --exit-code -- actions"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@aws-sdk/client-s3": "^3.1096.0",
    "@proof-computer/liskov-runtime": "github:proof-computer/liskov-runtime-js#89729b2a59d817a43fbae786bdcdcd4fce900fc3",
    "adm-zip": "^0.5.17"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.8",
    "@types/node": "^24.10.1",
    "esbuild": "^0.28.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "yaml": "^2.9.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "@proof-computer/liskov-runtime"
    ]
  }
}
